# ECCO / Clustering Toolbox — runtime dependencies only (no Jupyter/TensorFlow).
# Targets Python 3.11 through 3.14+ with flexible lower bounds so pip can resolve wheels.
#
# Recommended on Windows: use environment.yml (conda-forge) first, then optional pip extras.
#
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.3
biopython>=1.83
Pillow>=10
openpyxl>=3.1
xlrd>=2.0
XlsxWriter>=3.1
fpdf2>=2.7.0
selenium>=4.15
mplcursors>=0.5
tqdm>=4.65
networkx>=3.2
gseapy>=1.1
pydeseq2>=0.4
